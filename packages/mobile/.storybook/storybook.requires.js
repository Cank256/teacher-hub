/* do not change this file, it is auto generated by storybook. */

import {
  configure,
  addDecorator,
  addParameters,
  addArgsEnhancer,
  clearDecorators,
} from '@storybook/react-native';

import { decorators, parameters } from './preview';

if (decorators) {
  if (__DEV__) {
    // stops the warning from showing on every HMR
    require('@storybook/react-native').getStorybookUI({
      tabOpen: -1,
    });
  }
  decorators.forEach((decorator) => addDecorator(decorator));
}

if (parameters) {
  addParameters(parameters);
}

try {
  // Auto-generated stories
  const req = require.context(
    '../src/components',
    true,
    /\.stories\.(js|jsx|ts|tsx|mdx)$/
  );

  configure(() => {
    req.keys().forEach((filename) => req(filename));
  }, module);
} catch (e) {
  console.warn('Storybook stories could not be loaded:', e);
}

const getStories = () => {
  return {
    './src/components/ui/Button/Button.stories.tsx': require('../src/components/ui/Button/Button.stories.tsx'),
  };
};

const stories = getStories();

export { stories };
export const view = require('@storybook/react-native');